@"
{
  "name": "caldacerta",
  "version": "1.0.0",
  "private": true,
  "description": "CaldaCerta app",
  "scripts": {
    "start": "npm --prefix server start",
    "dev": "npm --prefix server run dev",
    "reset-produtos": "node scripts/reset-produtos.js",
    "build-index": "node scripts/build-simple-index.js",
    "fix-all": "npm run reset-produtos -- --serviceAccount=./serviceAccountKey.json --excelPath=./produtos.xlsx"
  },
  "dependencies": {
    "firebase-admin": "^13.6.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "firebase": "^10.7.0"
  }
}
"@ | Out-File -FilePath package.json -Encoding UTF8